set -g mouse on

# ------------------------------------------------------------------
# Search Mode VI
# ------------------------------------------------------------------
set-window-option -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -selection clipboard -i'
bind P paste-buffer
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel 'xclip -selection clipboard -i'

# ------------------------------------------------------------------
# Fix copy-mode highlighting to match Kitty theme
# ------------------------------------------------------------------
# set-window-option -g mode-style "fg=#575279,bg=#dfdad9"


# ------------------------------------------------------------------
# Vim-style Pane Controls (like Ctrl-w in Vim)
# ------------------------------------------------------------------

# --- Pane Navigation (h,j,k,l) ---
# We already set these, but here they are for completeness
bind-key -r h select-pane -L  # Go to the left pane
bind-key -r j select-pane -D  # Go to the down pane
bind-key -r k select-pane -U  # Go to the up pane
bind-key -r l select-pane -R  # Go to the right pane

# --- Pane Splitting (s, v) ---
bind-key s split-window -v  # 's' for horizontal split (Vim's s)
bind-key v split-window -h  # 'v' for vertical split (Vim's v)

# --- Pane Resizing (+, -, <, >) ---
bind-key -r + resize-pane -U 5  # Increase height
bind-key -r - resize-pane -D 5  # Decrease height
bind-key -r < resize-pane -L 5  # Decrease width
bind-key -r > resize-pane -R 5  # Increase width

# --- Pane Layouts (=, _, |) ---
bind-key = select-layout tiled    # '=' for equally high and wide
bind-key _ resize-pane -Z        # '_' for max out height (Zoom)
bind-key | resize-pane -Z        # '|' for max out width (Zoom)
# Note: In tmux, 'Zoom' (resize-pane -Z) is a toggle that maximizes
# both height AND width, which is why '_' and '|' do the same thing.

# --- Pane Management (o, q, T, w, x) ---
bind-key o kill-pane -a          # 'o' to Close all other panes
bind-key q kill-pane             # 'q' to Quit a pane
bind-key T break-pane            # 'T' to Break pane out into a new tab (window)
bind-key w select-pane -t :.+    # 'w' to Switch to next pane
bind-key x swap-pane -D          # 'x' to Swap current pane with next

# ------------------------------------------------------------------
# --- Enable True Color ---
# ------------------------------------------------------------------
set -g default-terminal "tmux-256color"
set-option -sa terminal-overrides ",xterm*:Tc"

# ------------------------------------------------------------------
# Additional modules reccommended by rose pine
# ------------------------------------------------------------------

set -g @rose_pine_host 'on' # Enables hostname in the status bar
set -g @rose_pine_hostname_short 'on' # Makes the hostname shorter by using tmux's '#h' format
set -g @rose_pine_date_time '%H:%M' # It accepts the date UNIX command format (man date for info)
set -g @rose_pine_user 'on' # Turn on the username component in the statusbar
set -g @rose_pine_directory 'off' # Turn on the current folder component in the status bar
set -g @rose_pine_bar_bg_disable 'on' # Disables background color, for transparent terminal emulators
# If @rose_pine_bar_bg_disable is set to 'on', uses the provided value to set the background color
# It can be any of the on tmux (named colors, 256-color set, `default` or hex colors)
# See more on http://man.openbsd.org/OpenBSD-current/man1/tmux.1#STYLES
set -g @rose_pine_bar_bg_disabled_color_option 'default'

set -g @rose_pine_only_windows 'off' # Leaves only the window module, for max focus and space
set -g @rose_pine_disable_active_window_menu 'on' # Disables the menu that shows the active window on the left

set -g @rose_pine_default_window_behavior 'on' # Forces tmux default window list behaviour
set -g @rose_pine_show_current_program 'on' # Forces tmux to show the current running program as window name
set -g @rose_pine_show_pane_directory 'on' # Forces tmux to show the current directory as window name
# Previously set -g @rose_pine_window_tabs_enabled

# Example values for these can be:
# set -g @rose_pine_left_separator ' > ' # The strings to use as separators are 1-space padded
# set -g @rose_pine_right_separator ' < ' # Accepts both normal chars & nerdfont icons
set -g @rose_pine_field_separator ' | ' # Again, 1-space padding, it updates with prefix + I
# set -g @rose_pine_window_separator ' - ' # Replaces the default `:` between the window number and name

# These are not padded
set -g @rose_pine_session_icon '' # Changes the default icon to the left of the session name
set -g @rose_pine_current_window_icon '' # Changes the default icon to the left of the active window name
set -g @rose_pine_folder_icon '' # Changes the default icon to the left of the current directory folder
set -g @rose_pine_username_icon '' # Changes the default icon to the right of the hostname
set -g @rose_pine_hostname_icon '󰒋' # Changes the default icon to the right of the hostname
set -g @rose_pine_date_time_icon '󰃰' # Changes the default icon to the right of the date module
set -g @rose_pine_window_status_separator "  " # Changes the default icon that appears between window names


# ------------------------------------------------------------------
# --- Plugins
# ------------------------------------------------------------------
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'rose-pine/tmux'
set -g @rose_pine_variant 'dawn'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

# setw mode-style "bg=#dfdad9"
